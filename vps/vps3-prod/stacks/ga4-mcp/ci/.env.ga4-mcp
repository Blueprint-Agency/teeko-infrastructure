# Rendered by CI -> /root/stacks/ga4-mcp/.env.ga4-mcp.
# The path matches where ci/credentials/sa-key.json lands, mounted read-only at
# /credentials by docker-compose.yml.
GOOGLE_APPLICATION_CREDENTIALS=/credentials/sa-key.json
GOOGLE_PROJECT_ID=@@GA4_MCP_PROJECT_ID@@
