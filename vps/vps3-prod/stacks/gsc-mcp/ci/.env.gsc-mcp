# Rendered by CI -> /root/stacks/gsc-mcp/.env.gsc-mcp.
# SKIP_OAUTH because this server authenticates with the service account key, not an
# interactive Google consent flow -- the OAuth in front of it is Dex's, not Google's.
GSC_CREDENTIALS_PATH=/credentials/sa-key.json
GSC_SKIP_OAUTH=true
